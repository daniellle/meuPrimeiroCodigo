import { Locatable } from '../archetyped/Locatable';
import { ObjectRef } from '../support/identification/ObjectRef';
/**
 * The concept of a named folder.
 */
export declare class Folder extends Locatable {
    /**
     * Sub-folders of this FOLDER.
     */
    folders: Folder[];
    /**
     * The list of references to other (usually) versioned objects logically in this folder.
     */
    items: ObjectRef[];
    constructor();
}
