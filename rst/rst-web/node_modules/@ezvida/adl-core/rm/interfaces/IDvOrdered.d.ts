import { CodePhrase } from '../CodePhrase';
import { ReferenceRange } from '../ReferenceRange';
import { IDvInterval } from './IDvInterval';
export interface IDvOrdered {
    normalRange: IDvInterval;
    otherReferenceRanges: ReferenceRange[];
    normalStatus: CodePhrase;
}
