import { Interval } from './Interval';
export declare class MultiplicityInterval extends Interval<number> {
    constructor();
    /**
     * Equal to '0..*' or '*'
     * @return MultiplicityInterval
     */
    static UNBOUNDED(): MultiplicityInterval;
    /**
     * True if this interval imposes no constraints, i.e. is set to 0..*
     * @returns {boolean}
     */
    isOpen(): boolean;
    /**
     * True if this interval expresses mandation, i.e. 1..1.
     * @returns {boolean}
     */
    isMandatory(): boolean;
    /**
     * True if this interval expresses optionality, i.e. 0..1.
     * @returns {boolean}
     */
    isOptional(): boolean;
    /**
     * True if this interval is set to 0..0.
     * @returns {boolean}
     */
    isProhibited(): boolean;
}
