import { DvMultimedia } from './datavalues/encapsulated/DvMultimedia';
import { Interval } from './Interval';
import { MultiplicityInterval } from './MultiplicityInterval';
export declare namespace RmObjectFactory {
    function createInterval<T>(lower: T, upper: T, lowerIncluded?: boolean, upperIncluded?: boolean): Interval<T>;
    function createMultiplicityInterval(lower: number, upper: number, lowerIncluded?: boolean, upperIncluded?: boolean): MultiplicityInterval;
    function createDvMultimedia(dataURI: string, alternateText?: string): DvMultimedia;
}
