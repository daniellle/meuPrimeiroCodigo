import { IFormObject } from '../rminfo/spec/IFormObject';
export declare abstract class RmObject implements IFormObject {
    multiple: boolean;
    required: boolean;
    aPath: string;
    amPath: string;
    cloned: boolean;
}
