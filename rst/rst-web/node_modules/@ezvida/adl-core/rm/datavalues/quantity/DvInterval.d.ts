import { IDvInterval } from '../../interfaces/IDvInterval';
import { IDvOrdered } from '../../interfaces/IDvOrdered';
import { DataValue } from '../DataValue';
export declare class DvInterval extends DataValue implements IDvInterval {
    lowerIncluded: boolean;
    upperIncluded: boolean;
    lowerUnbounded: boolean;
    upperUnbounded: boolean;
    lower: IDvOrdered;
    upper: IDvOrdered;
    constructor(init?: Partial<DvInterval>);
}
