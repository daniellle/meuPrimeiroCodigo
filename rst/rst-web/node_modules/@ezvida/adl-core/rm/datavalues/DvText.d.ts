import { IFormDvText } from '../../rminfo/spec/IFormDvText';
import { CodePhrase } from '../CodePhrase';
import { TermMapping } from '../TermMapping';
import { DataValue } from './DataValue';
import { DvUri } from './DvUri';
export declare class DvText extends DataValue implements IFormDvText {
    description: string;
    value: string;
    hyperlink: DvUri;
    formatting: string;
    mappings: TermMapping[];
    language: CodePhrase;
    encoding: CodePhrase;
    constructor(init?: Partial<DvText>);
}
