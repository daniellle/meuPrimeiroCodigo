import { Locatable } from '../rm/archetyped/Locatable';
export declare class RmQuery {
    private readonly POSITIONAL_PATTERN;
    private ROOT;
    private root;
    constructor(root: Locatable);
    findByAmPath(path: string): any[];
    findByAPath(path: string): any;
    private transform(path);
}
