import { Archetype } from '../am/Archetype';
import { CAttribute } from '../am/CAttribute';
import { CObject } from '../am/CObject';
import { IAmQuery } from './IAmQuery';
export declare class AmQuery implements IAmQuery {
    private ROOT;
    private readonly ATTRIBUTE;
    private readonly CHILDREN;
    private readonly PATTERN;
    private object;
    constructor(achertype: Archetype | CObject);
    static FIND(achertype: Archetype | CObject, amPath: string, agressive?: boolean): CObject | CAttribute | null;
    find(amPath: string, agressive?: boolean): CObject | CAttribute | null;
    private isCObject(arg);
    private transform(amPath, agressive?);
}
