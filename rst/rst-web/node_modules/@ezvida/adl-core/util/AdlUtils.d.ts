import { Archetype } from '../am/Archetype';
export declare namespace AdlUtils {
    const createFlatArchetypeClone: (source: Archetype) => Archetype;
    const getCRoot: (source: Archetype, path: string) => string;
}
