import { OperationalTemplate } from '../am/OperationalTemplate';
import { Template } from '../am/Template';
import { IArchetypeRepository } from '../flattener/IArchetypeRepository';
export declare class OperationalTemplateBuilder {
    private template;
    private repository;
    private presentTerminologies;
    private result;
    constructor(repository: IArchetypeRepository, template: Template);
    static BUILD(repository: IArchetypeRepository, template: Template): OperationalTemplate;
    private build();
    private walkAPath(cObject, aPath?, archetypeRef?);
    private expandTemplate();
    private expandCObject(context, cobj);
    private expandCComplexObject(context, cobj);
    private isProhibitedCAttribute(attribute);
    private isProhibitedCObject(cobj);
    private expandCAttribute(context, attribute);
}
