import { CodePhrase } from '../rm/CodePhrase';
import { RevisionHistory } from '../rm/generic/RevisionHistory';
import { ResourceAnnotations } from '../rm/ResourceAnnotations';
import { TranslationDetails } from '../rm/TranslationDetails';
import { ResourceDescription } from './ResourceDescription';
export declare abstract class AuthoredResource {
    originalLanguage: CodePhrase;
    isControlled: boolean;
    description: ResourceDescription;
    translations: {
        [key: string]: TranslationDetails;
    };
    revisionHistory: RevisionHistory;
    annotations: ResourceAnnotations;
}
