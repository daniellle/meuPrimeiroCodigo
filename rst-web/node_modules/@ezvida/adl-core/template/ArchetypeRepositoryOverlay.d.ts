import { IArchetypeRepository } from '../flattener/IArchetypeRepository';
import { Archetype } from '../am/Archetype';
export declare class ArchetypeRepositoryOverlay implements IArchetypeRepository {
    private delegate;
    private overlayArchetypeMap;
    private flattener;
    constructor(repository: IArchetypeRepository, archetypes: Archetype[]);
    getDifferentialArchetype(archetypeId: string): Archetype;
    getFlatArchetype(archetypeId: string): Archetype;
}
