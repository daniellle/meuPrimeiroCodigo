import { ITypedJsonSerializerCustomizer } from '../ITypedJsonSerializerCustomizer';
export interface IValidator<T> extends ITypedJsonSerializerCustomizer {
    validate(object: T): any;
}
export declare abstract class Validador<T> implements IValidator<T> {
    abstract validate(object: T): any;
    abstract getType(): Function;
    onSerializer(json: any): any;
}
