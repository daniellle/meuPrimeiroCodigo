import { Element } from '../../../rm/datastructures/Element';
import { Validador } from './IValidator';
export declare class RequiredValidator extends Validador<Element> {
    validate(object: Element): void;
    getType(): Function;
}
