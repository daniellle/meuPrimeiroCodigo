import { Token } from 'antlr4ts';
import { Map } from 'immutable';
import { OdinObjectValueContext, OdinValueContext } from '../../grammar/AdlParser';
export declare class OdinObject {
    private readonly startToken;
    private readonly properties;
    constructor(startToken: Token, properties: Map<String, OdinValueContext>);
    static PARSER(context: OdinObjectValueContext): OdinObject;
    getValue(property: string): OdinValueContext;
    tryGet(property: string): OdinValueContext;
    tryGetString(property: string): string;
    tryGetFloat(property: string): number | null;
    tryGetInteger(property: string): number | null;
}
