import { IDvOrdered } from './IDvOrdered';
export interface IDvInterval {
    lowerIncluded: boolean;
    upperIncluded: boolean;
    lowerUnbounded: boolean;
    upperUnbounded: boolean;
    lower: IDvOrdered;
    upper: IDvOrdered;
}
