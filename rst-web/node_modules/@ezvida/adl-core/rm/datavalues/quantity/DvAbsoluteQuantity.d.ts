import { DvAmount } from './DvAmount';
import { DvQuantified } from './DvQuantified';
/**
 * Abstract class defining the concept of quantified entities whose values are absolute with respect to an origin.
 * Dates and Times are the main example.
 */
export declare abstract class DvAbsoluteQuantity extends DvQuantified {
    constructor();
    abstract diff(other: DvAbsoluteQuantity): DvAmount;
}
