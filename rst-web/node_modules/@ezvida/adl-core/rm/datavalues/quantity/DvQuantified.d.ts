import { DvOrdered } from './DvOrdered';
/**
 * Abstract class defining the concept of true quantified values, i.e. values which are not only ordered,
 * but which have a precise magnitude.
 */
export declare abstract class DvQuantified extends DvOrdered {
    magnitudeStatus: string;
    abstract getMagnitude(): number;
}
