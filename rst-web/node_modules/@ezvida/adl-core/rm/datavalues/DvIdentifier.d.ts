import { DataValue } from './DataValue';
export declare class DvIdentifier extends DataValue {
    issuer: string;
    assigner: string;
    id: string;
    type: string;
    constructor(init?: Partial<DvIdentifier>);
}
