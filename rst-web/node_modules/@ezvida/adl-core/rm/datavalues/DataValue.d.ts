import { IFormConstraint } from '../../rminfo/spec/IFormConstraint';
import { RmObject } from '../RmObject';
import { IDataValue } from "../../spec/IDvValue";
export declare abstract class DataValue extends RmObject implements IFormConstraint<any>, IDataValue {
    constraint: any;
    hasValue(): boolean;
}
