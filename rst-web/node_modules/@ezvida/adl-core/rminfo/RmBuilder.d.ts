import { Archetyped } from '../rm/archetyped/Archetyped';
import { Locatable } from '../rm/archetyped/Locatable';
import { RmObject } from '../rm/RmObject';
export declare class RmBuilder {
    private root;
    private creator;
    constructor(root: Locatable);
    static TRAVERSE_ELEMENT_ARCHETYPE_DETAILS(root: Locatable, archetypeDetails?: Archetyped): void;
    /**
     * Add new clone element base on path
     * @param {string} path
     * @returns {any} new added element
     */
    addElement(path: string): Promise<RmObject>;
    walkNewDataValue(obj: {}): void;
    /**
     * Remove element by path
     * @param {Locatable} root
     * @param {string} path
     */
    removeElement(path: string): Promise<{}>;
    private setNewDataValue(element);
}
