import { TermBindingItem } from '../../am/TermBindingItem';
export interface IFormDvCodedText {
    codeList: [{
        code: string;
        text: string;
    }];
    externalTerm: boolean;
    termBindingItem: TermBindingItem[];
}
