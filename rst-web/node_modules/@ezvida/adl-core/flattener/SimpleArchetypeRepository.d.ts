import { IArchetypeRepository } from './IArchetypeRepository';
import { Archetype } from '../am/Archetype';
export declare class SimpleArchetypeRepository implements IArchetypeRepository {
    private archetypeIdDifferencialMap;
    private archetypeIdFlatMap;
    private flattener;
    constructor(archetypes: Archetype[]);
    private parseAndFlattenArchetype(archetypeId);
    getDifferentialArchetype(archetypeId: string): Archetype;
    getFlatArchetype(archetypeId: string): Archetype;
}
